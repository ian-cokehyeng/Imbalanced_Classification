Many resampling methods, many ML models, and many hyperparameters to tune
This notebook processes it all and identifies the winning combination in one clean swoop!


Data Loading

EDA

Baseline
	
Model Evaluation







df = pd.read_csv('credit_card.csv')


y = df.iloc[:, -1]
X = df.iloc[:, :-1]

df = pd.DataFrame(list(y), columns=['Target'])
num=(df.groupby('Target').size()/df.groupby('Target').size().sum())**2
PCC = num.sum()
print("Proportion Chance Criterion = {}%".format(100*PCC))
print("1.25*Proportion Chance Criterion = {}%".format(1.25*100*PCC))


2PM Est

